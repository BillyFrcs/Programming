public class Hewan {
     public void Run(Boolean isRun) {
          if (isRun)
               System.out.println("Animal can run.");
          else
               System.out.println("Animal can't run.");
     }
}
