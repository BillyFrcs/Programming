#include "Define.h"

int main()
{
     int result = COUNT(X);
     std::cout << "Count = " << result << std::endl;

     int max = MAXIMUM(A, B);
     std::cout << "MAXIMUM = " << max << std::endl;

     return 0;
}